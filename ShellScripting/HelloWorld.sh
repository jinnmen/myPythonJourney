#https://www.youtube.com/watch?v=cQepf9fY6cE
#! /bin/bash  <---find this by using "which bash" in command to find root folder for bash
echo "Hello World"

#ls -al in the bash root folder only allows write, read but not executable "x" by default hence need to use "chmod -x hello.sh" to make it executable.
# Next use "./hello.sh" to run script in command.